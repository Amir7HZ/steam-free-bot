name: Steam Free Games Bot

on:
  workflow_dispatch:
  schedule:
    # هر ۶ ساعت اجرا شود
    - cron: '0 */6 * * *'
    
    # همچنین هر روز ساعت ۱۲ ظهر ایران
    - cron: '30 8 * * *'  # ۸:۳۰ UTC = ۱۲:۰۰ ایران

jobs:
  check-games:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 دریافت کدها
      uses: actions/checkout@v3
    
    - name: 🐍 نصب پایتون
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📦 نصب کتابخانه‌ها
      run: pip install requests
    
    - name: 🚀 اجرای ربات
      run: python steam_bot.py
    
    - name: ✅ موفقیت
      if: success()
      run: |
        echo "✅ ربات با موفقیت اجرا شد!"
        echo "⏰ اجرای بعدی: ۶ ساعت دیگر"
        echo "🎮 بازی‌های رایگان به تلگرام ارسال شدند."
