name: Steam 100% OFF Games Bot

on:
  workflow_dispatch:
  schedule:
    # هر ۴ ساعت چک کن (بازی‌های ۱۰۰٪ تخفیف سریع تمام می‌شن!)
    - cron: '0 */4 * * *'
    
    # همچنین هر روز ۳ بار
    - cron: '0 8,16,0 * * *'  # ۸:۰۰، ۱۶:۰۰، ۰۰:۰۰ UTC

jobs:
  check-100-percent:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 دریافت کد
      uses: actions/checkout@v3
    
    - name: 🐍 نصب پایتون
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📦 نصب کتابخانه‌ها
      run: |
        pip install requests
        echo "📚 کتابخانه‌ها نصب شدند"
    
    - name: 🚀 اجرای ربات ۱۰۰٪ تخفیف
      run: |
        echo "🎯 شروع جستجوی بازی‌های ۱۰۰٪ تخفیف..."
        python steam_bot.py
    
    - name: ✅ تأیید اجرا
      if: success()
      run: |
        echo "✅ ربات با موفقیت اجرا شد!"
        echo "🎮 بازی‌های ۱۰۰٪ تخفیف بررسی شدند"
        echo "⏰ دفعه بعد: ۴ ساعت دیگر"
